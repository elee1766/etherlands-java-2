package etherlandscore.etherlandscore.slashcommands.helpers;

public enum SlashCommands {
  infoLocal, infoGiven, setPlayer, setGroup, setAllGroup, update,
  forceUpdate, reclaim, reclaimLocal, help, delegateLocal,
  delegate, toggleTeam, toggleLocal, toggleGlobal, relink, districtPlayer, districtGroup, friendAdd, friendAddSelector, friendRemove, friendRemoveSelector, list, suicide, rules, remove, add, delete, info, create, auto, coord, map, idLocal, plot, idGiven, deleteTeam, kick, kickOwner, leave, invite, join, moveAlerts, tradeMenu, addItem, approve, pay, mint, balanceSelf, sendGlobal
}
